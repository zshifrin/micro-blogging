<div class="container">
	<div class="row">
		<% @posts.each do |p| %>
		<div class="col-md-3 col-md-offset-1 home-posts box-shadow">  
		    	<h4><%= p.user.username %></h4>
		        <p><%= p.content %></p>
		</div>
		<% end %>
	</div>
</div>
